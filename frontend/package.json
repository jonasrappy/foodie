{
  "name": "foodie-avatar",
  "private": true,
  "version": "1.3.0",
  "type": "module",
  "scripts": { "build": "esbuild foodie-avatar.js --bundle --minify --format=esm --target=chrome84 --outfile=../public/foodie-3d.js --legal-comments=eof" },
  "dependencies": { "three": "0.186.0" },
  "devDependencies": { "esbuild": "0.28.2" }
}
